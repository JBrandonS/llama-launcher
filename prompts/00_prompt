
llama-launcher plans to be a powerful CLI tool for managing and running LLaMA.cpp models. It will provide a user-friendly interface for downloading, configuring, and launching LLaMA models with ease. The tool will support various features such as:
listing available models, downloading models from specified sources, configuring model parameters, and launching the models with custom settings. Additionally, it will include options for managing multiple models and providing detailed logs for troubleshooting. The goal is to simplify the process of working with LLaMA.cpp models and make it accessible to a wider audience.

Furthermore, llama-launcher will be able to benchmark the performance of different models and configurations, allowing users to optimize their setups for specific use cases. 

This tool will use an advanced interactive CLI interface, making it easy for users to navigate through the various options and settings. It will also include comprehensive documentation and support resources to assist users in getting the most out of their LLaMA models.

When running llama-launcher without arguments it should display only existing models found in configurable directories including the default cache paths and huggingface hub download location for llama cpp. The user should be able to select a model to run from the list of available models, and the tool will handle the necessary configurations and launch the model with the selected settings. This feature will allow users to quickly switch between different models and configurations without having to manually manage files or settings. The launch should also include options for customizing the launch parameters, such as specifying the number of threads, enabling GPU support, and adjusting memory usage. This will provide users with the flexibility to optimize their model performance based on their specific hardware capabilities and requirements.

Running models should be done in the background by default, allowing users to continue using their terminal for other tasks while the model is running. The tool should also provide options for monitoring the status of running models, including viewing logs and performance metrics in real-time. This will help users keep track of their models' performance and troubleshoot any issues that may arise during execution.

There should be an option to look at the model card from hugging face hub for the selected model, providing users with detailed information about the model's architecture, training data, and performance metrics. This will help users make informed decisions about which models to use for their specific applications and understand the capabilities and limitations of each model. It should also be parsed for recommended best settings for example for the unsloth/gemma-4-E4B-it-GGUF it would be processing <https://huggingface.co/unsloth/gemma-4-E4B-it-GGUF> and <https://unsloth.ai/docs/models/gemma-4> through some bootstrapped model (or the current model once selected) and these would tell you things such as Use the following standardized sampling configuration across all use cases: temperature=1.0 top_p=0.95 top_k=64 to configure the launch settings for that model.

When ran with the list-models argument, llama-launcher should display a list of all available models found in the configured directories. This list should include details such as the model name, version, and source location. Users can then select a model from this list to view more information or to launch it directly from the interface. This feature will help users easily identify and manage their available models, making it simpler to choose the right one for their needs. Additionally, the tool could provide options for filtering and sorting the list of models based on various criteria, such as model size, performance metrics, or source type.

The launch will also feature a user-friendly interface for searching huggingface hub for available LLaMA models. Users can search for specific models by name, version, or other attributes, and the tool will display the relevant results. This will allow users to easily find and download new models directly from the Hugging Face Hub without having to navigate through the website manually. The search functionality could also include filters to narrow down results based on model size, performance, or other criteria, making it easier for users to find the most suitable model for their needs.

Additionally the launcher should support running a benchmarking suite to evaluate the performance of different models and configurations. Users can select specific benchmarks to run, and the tool will execute the tests while providing detailed performance metrics such as inference time, memory usage, and throughput. This feature will help users optimize their model setups and make informed decisions about which models to use for their specific applications. The benchmarking suite could also include options for comparing multiple models side by side, allowing users to easily identify the best performing model for their needs.

The launch should have a method of saving configurations for different models and settings, allowing users to quickly switch between them without having to manually reconfigure each time. This could be implemented through a configuration file or a built-in configuration management system within the tool. Users would be able to save their preferred settings for each model, including parameters such as thread count, GPU support, and memory usage. This feature will enhance the user experience by providing a convenient way to manage and switch between different configurations based on their specific requirements.

Finally the model should support creating a startup daemon that can automatically launch a specified model with predefined settings when the system starts. This would be particularly useful for users who want to have their LLaMA models running continuously without having to manually start them each time. The daemon could be configured to run in the background and automatically restart if it encounters any issues, ensuring that the model remains available and operational at all times. This feature would provide added convenience and reliability for users who rely on their LLaMA models for various applications.

Overall, llama-launcher aims to be a comprehensive and user-friendly tool for managing and running LLaMA.cpp models, providing a wide range of features to enhance the user experience and optimize model performance.