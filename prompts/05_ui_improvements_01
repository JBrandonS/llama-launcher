for the launch settings page make context size slider run from 0 to 128k, make sure the repeate last N, num predict, num keep, and all the advanced settings are defaulted to the defaults for llama.cpp. Then create a template for me that recreates the launch commands in the ~/lamma/run_* scripts, ignoring run_all. Once created ensure that these are able to be loaded into the launch page, where for each model if existing templates are found a selection of found templates should show under the model & port section. This should default to the last used template or the most recent file if not used or the first. Ensure that this section is not shown if no existing templates are found.